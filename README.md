# 🏠 تحليل أسعار المنازل – مشروع تعلم آلة شامل

مشروع تحليل بيانات أسعار المنازل وبناء نموذج **انحدار خطي** للتنبؤ بالسعر بناءً على عدة خصائص مثل المساحة، عدد الغرف، الحمامات، وجود مرافق إضافية، وغيرها.

---

## 📝 وصف المشروع

يهدف هذا المشروع إلى تحليل بيانات أسعار المنازل واستخلاص رؤى قيمة حول العوامل المؤثرة في السعر. كما يتم بناء نموذج تعلم آلة قادر على تقدير سعر المنزل بدقة مقبولة.

### المتغيرات المستقلة (المدخلات):
- `area`: مساحة المنزل (قدم مربع)
- `bedrooms`: عدد غرف النوم
- `bathrooms`: عدد الحمامات
- `stories`: عدد الطوابق
- `mainroad`: هل المنزل يقع على شارع رئيسي؟
- `guestroom`: هل يوجد غرفة ضيوف؟
- `basement`: هل يوجد قبو؟
- `hotwaterheating`: هل يوجد سخان مياه مركزي؟
- `airconditioning`: هل يوجد تكييف؟
- `parking`: عدد أماكن وقوف السيارات
- `prefarea`: هل المنزل في منطقة مفضلة؟
- `furnishingstatus`: حالة الأثاث (مفروش، نصف مفروش، غير مفروش)

### المتغير الهدف:
- `price`: سعر المنزل

---

## 📁 محتويات المشروع

- `Housing.csv` – مجموعة البيانات الأولية.
- `Housing.ipynb` – دفتر Jupyter الذي يحتوي على جميع خطوات التحليل والمعالجة والنمذجة.
- `README.md` – هذا الملف.

---

## 🔍 خطوات التحليل

### 1. تحميل البيانات واستكشافها الأولي
- استخدام `pandas` لقراءة البيانات (`pd.read_csv`).
- استعراض المعلومات العامة `info()` ووصف إحصائي `describe()`.
- فحص القيم الفريدة لكل عمود (`nunique()`).
- التأكد من عدم وجود قيم مفقودة (جميع الأعمدة خالية من القيم الفارغة).

### 2. التحليل الاستكشافي (EDA)
تم استخدام مكتبات `matplotlib` و `seaborn` لإنشاء رسوم بيانية متنوعة:
- **مخططات أعمدة** لتوزيع عدد الغرف، الحمامات، مواقف السيارات، الطوابق، إلخ.
- **مخططات دائرية** للنسب المئوية للمتغيرات الثنائية (مثل: وجود غرفة ضيوف، قبو، تكييف).
- **مخططات التوزيع** للسعر والمساحة (Histogram).
- **مخطط مبعثر** يوضح العلاقة بين المساحة والسعر مع خط انحدار.
- **مصفوفة الارتباط** لفهم العلاقات بين المتغيرات الرقمية.

### 3. معالجة البيانات
- تم تحويل المتغيرات النصية (مثل `yes`/`no`) إلى أرقام باستخدام **One-Hot Encoding** عبر `pd.get_dummies()`.
- تم فصل المتغيرات المستقلة (`X`) عن المتغير التابع (`y`).

### 4. بناء النموذج
- **تقسيم البيانات**: 80% تدريب، 20% اختبار (`train_test_split`).
- **تطبيع البيانات**: استخدام `StandardScaler` لقياس المتغيرات الرقمية.
- **النموذج**: `LinearRegression` من مكتبة `sklearn`.

### 5. تقييم النموذج
تم استخدام عدة مقاييس لتقييم أداء النموذج:
- **R² Score**: يوضح نسبة التباين المفسر.
- **Mean Absolute Error (MAE)**: متوسط الخطأ المطلق.
- **Mean Squared Error (MSE)**: متوسط مربعات الأخطاء.
- **Root Mean Squared Error (RMSE)**: الجذر التربيعي لـ MSE.
- **Mean Absolute Percentage Error (MAPE)**: متوسط نسبة الخطأ المطلقة.

### 6. تحسين النموذج (اختياري)
تمت تجربة استخدام **Polynomial Features** بدرجة 2 لتحسين الأداء، مما أدى إلى تحسن ملحوظ في دقة النموذج.

---

## 📊 النتائج

### النموذج الخطي الأساسي
- **R² Score**: ~63%
- **MAE**: ~840,205
- **RMSE**: ~1,216,509

### النموذج متعدد الحدود (درجة 2)
- **R² Score**: ~81%
- **MAE**: ~564,274
- **RMSE**: ~851,330
- **MAPE**: ~18.7%

✅ النموذج المحسن يفسر حوالي **81%** من التباين في السعر، مما يشير إلى أداء جيد.

---

## ⚙️ كيفية التشغيل

1. **استنساخ المستودع**
   ```bash
   git clone https://github.com/your-username/House-Price-Prediction-Project.git
   cd House-Price-Prediction-Project
